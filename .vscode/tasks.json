{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",

                "${file}",  // current file compilation

                // "${fileDirname}/main.cpp",
                // "${fileDirname}/DateTime.cpp",
                // "${fileDirname}/Meetings.cpp",
                // "${fileDirname}/Yacht.cpp",
                // "${fileDirname}/ProtossAirShip.cpp",
                // "${fileDirname}/TerranAirShip.cpp",
                // "${fileDirname}/Viking.cpp",
                // "${fileDirname}/AirShip.cpp",
                // "${fileDirname}/BattleField.cpp",
                // "${fileDirname}/main.cpp",
                
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}